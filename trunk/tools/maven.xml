<?xml version="1.0"?>
<project default="build" xmlns:maven="jelly:maven">

    <postGoal name="clean:clean">
        <maven:maven descriptor="${basedir}/src/plugin-test/project.xml"
            goals="clean:clean"
            ignoreFailures="false"/>
    </postGoal>

    <goal name="build" prereqs="plugin:install"/>

    <goal name="deploy" prereqs="plugin:deploy"/>

    <postGoal name="plugin:install">
<!--
        <attainGoal name="plugin:test"/>
-->
    </postGoal>

</project>